// Real function
// import { PixelRatio } from 'react-native';
// export const dp = (sizeInPixels) =>
//   PixelRatio.getPixelSizeForLayoutSize(sizeInPixels);

export const dp = (sizeInPixels) => sizeInPixels;
